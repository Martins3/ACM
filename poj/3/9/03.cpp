#include <iostream>
#include <fstream>
#include <vector>
#include <map>
#include <cstring>
#include <algorithm>

using namespace std;
#define VSCODE_READ freopen("../../../input.txt", "r", stdin);
#define C_READ freopen("input.txt", "r", stdin);

/**
 * d 数组: d 
 * g 数组: k  k的最小下标
 */
#define INF 1000000
int main(){
    VSCODE_READ

    int N;
    while(scanf("%d", &N)){
        vector<int> num;
        for(int i = 0; i < N; i++){
            num.push_back(i);
        }
        vector<int> g;
        
        vector<int> d;
        
    }
    return 0;
};